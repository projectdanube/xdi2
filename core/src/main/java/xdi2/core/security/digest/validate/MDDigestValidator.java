package xdi2.core.security.digest.validate;

import xdi2.core.features.digests.MDDigest;

/**
 * This is a DigestValidator that can validate an XDI MDDigest.
 */
public interface MDDigestValidator extends DigestValidator<MDDigest> {

}
